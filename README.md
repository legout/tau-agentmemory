# tau-agentmemory

A Tau extension that exposes agentmemory's REST API as native `memory_*` tools.

The repository currently contains the approved specification, architecture decision, and implementation plan for the REST bridge.
